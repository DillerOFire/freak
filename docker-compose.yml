# Per-instance deployment file.
# Copy .env into the deploy dir (or set env_file path) and adjust the host
# port for the Telegram Web App listener. Each instance runs from the same image.
services:
  bot:
    build: .
    # Or pull a published image, e.g. ghcr.io/your-org/freak:latest
    container_name: freak
    restart: unless-stopped
    env_file: .env
    environment:
      RUN_MODE: docker
      ENV_FILE: /data/.env
      WEB_SETTINGS_HOST: "0.0.0.0"
    volumes:
      - ./data:/data
    ports:
      - "127.0.0.1:${WEB_SETTINGS_PORT:-8780}:${WEB_SETTINGS_PORT:-8780}"
